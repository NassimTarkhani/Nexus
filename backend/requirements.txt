# NEXUS Backend — FastAPI (Python 3.14 compatible)
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.11
pydantic-settings>=2.9

# Database
sqlalchemy>=2.0
asyncpg>=0.30

# Auth — PyJWT instead of python-jose (better Python 3.14 support)
PyJWT[crypto]>=2.9
python-multipart>=0.0.20

# Supabase — use httpx to call Supabase REST API directly
httpx>=0.28

# AI / RAG
openai>=1.84
pdfplumber>=0.11

# Utils
python-dotenv>=1.1
structlog>=25.4
